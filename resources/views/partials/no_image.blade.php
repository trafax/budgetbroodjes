<svg xmlns="http://www.w3.org/2000/svg" width="{{ $width }}" height="{{ $height }}" viewBox="0 0 {{ $width }} {{ $height }}">
    <rect fill="#868e96" width="{{ $width }}" height="{{ $height }}"/>
    <text fill="#dee2e6" font-size="16" dy="10.5" x="50%" y="50%" text-anchor="middle">{!! $text ?? 'Geen afbeelding' !!}</text>
</svg>
